{
  "name": "example-addon",
  "version": "1.0.0",
  "description": "Short example for using C++ with node",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guilherme Nogara",
  "license": "ISC",
  "devDependencies": {
    "node-gyp": "^7.0.0"
  },
  "dependencies": {
    "node-addon-api": "*"
  }
}
